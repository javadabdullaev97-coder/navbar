{
  "name": "navbar",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "dev:web": "pnpm --filter @navbar/web dev",
    "test:core": "pnpm --filter @navbar/core test",
    "test:rls": "bash supabase/tests/run_isolation_test.sh"
  }
}
